# Task

`parse_timestamp` must accept timezone-aware ISO 8601 timestamps using either a normal UTC offset or the `Z` UTC suffix, and it must continue rejecting naive timestamps.

The project targets Python 3.12. Do not add third-party dependencies.

Run the repository tests before finishing.
