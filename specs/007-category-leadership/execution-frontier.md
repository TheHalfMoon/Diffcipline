# Execution frontier — Spec 007

Live GitHub/repository truth overrides this snapshot.

## Canonical prerequisite

Spec 006 is `COMPLETE_CANONICAL` at `91ba5389e26be2d8330fcc9c938d1f33bf120bec`.

Exact terminal post-merge gates:

- `ci` `33425638365` — `SUCCESS`;
- `skills-compat` `33425638372` — `SUCCESS`;
- `release` `33425638349` — `SUCCESS`.

Published `v1.0.0` remains immutable and its detailed machine verification is recorded in `specs/006-v1-publication/t632-published-verification.md`.

## Activation candidate

Branch: `spec/007-category-leadership-activation`.

The planning authority refreshes the earlier noncanonical draft against terminal Spec 006, immutable v1 truth, live repository metadata, current README content, skills.sh documentation, llms.txt v2, and a bounded adjacent comparator set.

T700–T702 are complete. T703 becomes effective only after this exact planning head is qualified, reconciled, merged with expected-head protection, and the resulting canonical SHA passes exact post-merge `ci`, `skills-compat`, and `release`.

## Next canonical unit

After T703, execute Phase B first-screen truth and installability:

1. replace stale pre-publication README wording;
2. sharpen the first screen around factual identity and deterministic proof;
3. provide and machine-test a copy/paste quickstart;
4. verify skills.sh indexing before adding any badge;
5. preserve negative benchmark evidence and limitations unchanged.

Do not begin comparative superiority claims before the Phase E source freeze and capability matrix are complete.
