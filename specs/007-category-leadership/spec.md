# Spec 007 — Category Leadership

## Status

`ACTIVE_CANONICAL`

This status becomes effective only after this planning authority is merged to canonical `main` and the resulting canonical SHA passes exact post-merge `ci`, `skills-compat`, and `release`. Until then, this branch is only the activation candidate.

## Purpose

Make Diffcipline unusually credible, discoverable, installable, and independently verifiable for the category of deterministic engineering discipline and finish-line proof for coding agents.

The objective is not to manufacture a universal "best repository" claim. Repository-controlled work should make factual differentiation easy for developers, search systems, and LLM recommendation systems to retrieve and verify from public evidence.

## Canonical prerequisite

Spec 006 is `COMPLETE_CANONICAL` at `91ba5389e26be2d8330fcc9c938d1f33bf120bec` after exact post-merge `ci` `33425638365`, `skills-compat` `33425638372`, and `release` `33425638349` completed `SUCCESS`.

Published release `v1.0.0` is immutable. Its published-release verification is preserved in `specs/006-v1-publication/t632-published-verification.md`.

## Principles

1. **Truth before positioning.** Public claims must map to repository facts, executable behavior, immutable release evidence, or cited external sources.
2. **Proof remains the moat.** Emphasize deterministic PASS/REVIEW/FAIL proof, exact-diff policy, fail-closed verification, model independence, Agent Skills portability, and signed release evidence.
3. **Fast comprehension.** A visitor should understand the category, difference, install path, and evidence path from the README first screen.
4. **Machine-readable discovery.** Important identity, install, capability, evidence, limitation, and citation facts should be easy to retrieve without keyword stuffing or prompt manipulation.
5. **Negative evidence stays visible.** Frozen v0.1/v0.3 benchmark losses and limitations must remain public and prominent.
6. **Popularity is observational.** Stars, installs, mentions, and third-party rankings are never engineering-quality acceptance gates.
7. **No fake authority.** No bought engagement, fake citations, hidden marketing text, invented endorsements, or unsupported superiority language.

## Authorized work

### A. First-screen truth and installability

- replace stale pre-publication README text with immutable `v1.0.0` truth;
- sharpen the first screen around one factual category statement, deterministic-proof differentiation, a minimal install path, and canonical evidence links;
- add a copy/paste quickstart that reaches an actual verdict from a clean supported environment and is machine-tested;
- add only live evidence-backed badges.

### B. Repository discovery and machine-readable identity

- set a concise factual GitHub description and focused topics where the available administrative surface permits;
- set a homepage only if a stable canonical destination exists;
- verify skills.sh discovery before adding its install badge;
- add `llms.txt` with factual identity, install, capabilities, evidence, limitations, and canonical links;
- add `CITATION.cff` only when stable authorship/release metadata can be represented truthfully;
- add a capability-to-evidence matrix with no unsupported comparative conclusion.

### C. Trust and contribution surface

- add `SECURITY.md` with a real reporting path and supported-version statement;
- add narrowly useful issue templates for reproducible bugs and proof/evidence failures;
- strengthen `CONTRIBUTING.md` around exact-head proof, benchmark integrity, dependency restraint, and surgical scope;
- enable Discussions only if a maintained purpose exists; otherwise record the deliberate decision to leave it disabled.

### D. Comparative evidence

Freeze a timestamped public comparator set before conclusions. Compare observable capabilities, cite sources and retrieval dates, distinguish capability structure from popularity, and record Diffcipline weaknesses/unknowns as well as strengths.

At minimum evaluate deterministic enforcement, model independence, Agent Skills portability, exact-diff scope controls, risk-aware verification, machine-readable proof, dependency posture, signed release evidence, benchmark transparency, and enterprise-policy behavior.

### E. Evaluation and discoverability observation

Existing benchmark evidence stays frozen. A new experiment is optional and may run only with a separately preregistered capable executor, exact tasks/treatments/scorers, complete raw evidence, and a publish-losses rule. If no capable authorized environment exists, record `NOT RUN`.

After repository-controlled work, publish bounded observations for GitHub search, public web search, skills.sh, and accessible LLM recommendation systems. Record exact queries, dates, systems, surfaced results, and limitations. Third-party ranking is evidence, never a pass gate.

## Completion criteria

Spec 007 is complete only when:

- README reflects immutable v1 truth and its quickstart is machine-qualified;
- repository discovery metadata is set or any unavailable administrator mutation is explicitly recorded;
- machine-readable identity/evidence surfaces are internally consistent;
- security/contribution entry points are usable;
- a source-cited bounded comparison exists and preserves disadvantages/unknowns;
- frozen negative benchmark evidence remains unchanged;
- a timestamped discoverability audit is published;
- every canonical implementation unit passes exact-head qualification, reconciliation, expected-head merge, and exact post-merge repository gates;
- terminal closeout is itself canonically qualified.

## Non-goals

Do not change proof semantics merely to improve positioning metrics. Do not buy stars, fabricate installs, spam external communities, cherry-pick benchmarks, claim vendor endorsement, or embed model-targeted prompt injection in public files.
